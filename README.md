# Voting Management System

A simple **voting database management system** built as a school assignment using Node.js, Express, EJS, and SQL. The project demonstrates CRUD operations, basic routing, templating, and database integration.

---

## 🚀 Overview

This system allows you to manage election-related data through a web interface. It includes templates, backend logic, and a SQL schema (`malawi_electoral_comition.sql`) for database setup.

---

## 🧠 Tech Stack

* Node.js & Express
* EJS templating
* HTML / CSS / JavaScript
* SQL database schema

---

## 📁 Project Structure

```
/
├── app.js
├── package.json
├── malawi_electoral_comition.sql
├── public/       # CSS, images, assets
└── views/        # EJS templates
```

---

## 💻 Running the Project

```bash
git clone https://github.com/Vera-Mhango/Voting-Management-System.git
npm install
npm start
```

Then visit:

```
http://localhost:3000
```

Import the provided `.sql` file to create the sample database.

---

## 🎯 Purpose of the Assignment

This project was created for academic learning to practice:

* Building a backend with Express
* Using EJS for server-side rendering
* Connecting Node.js to a SQL database
* Working with forms and CRUD operations

---

## 📝 Notes

You can extend this project with features like authentication, role-based access, or a fully deployed database.
